apiVersion: v1
kind: Secret
metadata:
  name: linked-data-secrets
  namespace: web
  labels:
    app: linked-data-service
type: Opaque
stringData:
  # IMPORTANT: This is a template. Create actual secrets using:
  # kubectl create secret generic linked-data-secrets \
  #   --from-literal=geonames_username=YOUR_USERNAME \
  #   -n web

  # Geonames API credentials
  geonames_username: "REPLACE_WITH_ACTUAL_USERNAME"
