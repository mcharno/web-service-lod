apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: linked-data

resources:
  - namespace.yaml
  - configmap.yaml
  - secret-template.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

labels:
  - pairs:
      app.kubernetes.io/name: linked-data-service
      app.kubernetes.io/managed-by: argocd

images:
  - name: ghcr.io/GITHUB_USERNAME/linked-data-service
    newTag: latest
